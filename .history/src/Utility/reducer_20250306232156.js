
export const initialState = {
    basket: [],
  };
  
  export const reducer = (state, action) => {
    switch (action.type) {
      case 'ADD_TO_BASKET':
        //check the item exists
        cons


      default:
        return state;
    }
  };
  